REM [Bruce compat rewrite] Original Hak5 demo gated a STRINGLN on the
REM internal variable $_CAPSLOCK_ON via IF/END_IF. Bruce's ducky_typer.cpp
REM has no IF/conditional support and does not expose any $_CAPSLOCK_ON
REM (or other host-keyboard-LED state) variable at all — there's no way
REM to know the real caps-lock state to decide whether to type the
REM message, and guessing would misrepresent what the script does. This
REM is a pure language-feature demo, not an operational payload, so the
REM conditional body is left un-executed below (kept only as documentation).
REM Example of optimized IF statement
REM with internal variable
REM IF $_CAPSLOCK_ON THEN
REM     STRINGLN The caps lock key is on
REM END_IF
REM The internal variable $_CAPSLOCK_ON is checked.
REM If it evaluates as TRUE, the message "The caps lock key is on" is typed.
